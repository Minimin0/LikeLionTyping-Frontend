VITE_API_BASE_URL=http://localhost:8080/api

# 로컬에 실제 백엔드를 띄울 수 있을 때만 .env.local에 설정한다.
# 설정하면 vite.config.js의 dev 서버 Mock 대신 이 주소로 프록시된다.
# 비워두면(기본값) VITE_API_BASE_URL도 비워서 상대경로 /api로 요청하고,
# vite.config.js의 Mock API가 대신 응답한다.
# DEV_API_TARGET=http://localhost:8080
