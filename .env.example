# 공통 API 경로. 운영에서는 Nginx가 /api 요청을 백엔드로 전달한다.
VITE_API_BASE_URL=/api
# 개발 연습용 데이터 활성화. 실제 백엔드 연결 시 false로 변경한다.
VITE_USE_MOCK=true
# 실제 백엔드 연결 시 개발 서버가 요청을 전달할 주소. /api를 붙이지 않는다.
API_PROXY_TARGET=http://localhost:8080
