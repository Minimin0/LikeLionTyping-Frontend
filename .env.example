VITE_API_BASE_URL=http://localhost:8080

# Dev-only: set to "true" to exercise the Admin UI against MSW fixtures
# (src/mocks/) when the real backend isn't reachable yet. Leave unset/false
# for real integration testing. Never enable this in a production build.
VITE_USE_MSW=false
